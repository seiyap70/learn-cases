# 架构决策记录（ADR）模板

> ADR（Architecture Decision Record）是记录重要架构决策的轻量级文档。
> 每个重大技术选型、架构模式变更、关键取舍都应写一份 ADR。

## 为什么写 ADR？

- **决策可追溯** —— 几个月后回看，知道"为什么选了 Kafka 而不是 RabbitMQ"
- **避免重复争论** —— 同一个问题不需要反复讨论，有记录可查
- **新成员快速理解** —— 通过 ADR 集合可以理解系统演进的脉络
- **强制思考** —— 写 ADR 的过程本身就是对决策的审视

## ADR 模板

```markdown
# ADR-{编号}: {标题}

## 状态

{提议 | 已接受 | 已废弃 | 已替代}

## 上下文

描述触发此决策的问题和背景。包括：
- 当前遇到什么问题？
- 有哪些约束条件？
- 不做决策的后果是什么？

## 决策

我们决定采用 {方案X}，因为 {理由}。

## 考虑的方案

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| 方案A | ... | ... | ... |
| 方案B | ... | ... | ... |
| 方案C | ... | ... | ... |

选择方案X的原因：{详细说明权衡过程}

## 影响

此决策带来的正面和负面影响：

**正面影响：**
- ...

**负面影响：**
- ...

**需要后续跟进：**
- ...

## 相关案例

- {关联的架构设计案例编号}

## 参考

- {外部文档/文章链接}
```

## ADR 示例

### ADR-001: 选择 Redis GEO 作为实时位置存储

**状态**：已接受

**上下文**：
网约车平台需要存储 30 万在线司机的实时位置，支持 3km 范围内的空间查询。位置数据每 5 秒更新一次，查询延迟要求 < 100ms。

**决策**：
选择 Redis GEO（Sorted Set + GeoHash）存储实时位置，PostgreSQL PostGIS 存储商家静态位置。

**考虑的方案**：

| 方案 | 优点 | 缺点 |
|------|------|------|
| Redis GEO | 毫秒级读写，天然支持范围查询 | 内存成本高，数据不持久 |
| PostgreSQL PostGIS | 持久化，丰富 GIS 函数 | 写入 QPS 受限（< 5000） |
| MongoDB 地理索引 | 灵活文档模型 | 位置更新延迟高 |
| 自建 GeoHash + Cassandra | 可水平扩展 | 开发成本高，维护复杂 |

选择 Redis GEO 的理由：实时位置数据更新频繁（6万 QPS 写入），但不需要持久化（司机离线即清除），Redis GEO 完美匹配此场景。

**影响**：
- 正面：实时位置读写极快，查询延迟 < 5ms
- 负面：Redis 宕机后需重建位置数据（司机重新上报即可）
- 后续：需要实现 Redis 集群高可用（哨兵/Cluster）

**相关案例**：C09

## ADR 编写时机

- 当你需要做**不可轻易逆转**的技术决策时
- 当有**多个可行方案**需要权衡取舍时
- 当决策会影响**多个团队/多个服务**时
- 当你发现**之前的决策需要推翻**时（写新 ADR 替代旧 ADR）

## ADR 编写原则

1. **短小精悍** —— 1 页以内，不要写成长篇文档
2. **聚焦 WHY** —— 记录"为什么选 A 不选 B"，而非"选了 A"
3. **不可修改** —— ADR 一旦写定不修改内容，需要推翻时写新 ADR 并标记旧 ADR 为"已替代"
4. **按时间排列** —— ADR 编号按时间递增，形成决策时间线
5. **放在一起** —— 所有 ADR 放在项目 `docs/adr/` 目录下