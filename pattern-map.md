# 架构模式图谱

> 跨案例的架构模式索引，帮助你发现不同业务场景中重复出现的设计模式。
> 理解模式比记忆方案更重要——模式可以在不同场景中灵活组合。

## 模式与案例映射

### 一、流量治理模式

处理高并发、流量洪峰的核心模式。

| 模式 | 核心思路 | 适用场景 | 案例 |
|------|---------|---------|------|
| 层层过滤 | 请求逐层过滤，越内层流量越小 | 流量远超处理能力 | C01, C14, C17 |
| 限流降级 | 超出容量时快速失败，保护核心 | 防止雪崩 | C01, C17 |
| 背压控制 | 下游压力反馈到上游，主动减速 | 生产者-消费者场景 | C05, C14 |
| 熔断器 | 连续失败时断开调用，定时半开试探 | 服务间调用 | C02, C10 |
| 流量染色 | 给请求打标，按标签路由（灰度/A/B测试） | 灰度发布 | C08, C21, C25 |

**层层过滤的典型层次（以 C01 秒杀为例）：**
```
100% 流量 → 前端随机过滤(98%) → 2% → Nginx限流(50%) → 1% → 网关校验(60%) → 0.6% → Redis预扣 → MySQL
```

### 二、数据一致性模式

分布式系统中保证数据正确性的核心模式。

| 模式 | 核心思路 | 一致性级别 | 案例 |
|------|---------|-----------|------|
| 2PC | 两阶段提交，全局锁 | 强一致 | （反例，案例中未使用） |
| TCC | Try-Confirm-Cancel，预留资源 | 最终一致 | C02 |
| Saga | 正向+补偿，链式执行 | 最终一致 | C02, C16 |
| 事件溯源 | 事件追加，状态由事件推导 | 强一致（单聚合） | C07, C13 |
| CRDT | 数据结构本身保证收敛 | 最终一致 | C06 |
| 乐观锁 | 版本号校验，冲突重试 | 强一致（单行） | C07 |
| 幂等消费 | 唯一键去重，重复请求不重复执行 | - | C02, C07, C16 |

**一致性模式选择决策树：**
```
需要强一致？
├─ 是 → 单服务内？
│      ├─ 是 → 乐观锁 / 事件溯源
│      └─ 否 → 能接受性能损失？ → 2PC / TCC
└─ 否 → 最终一致可接受？
       ├─ 是 → 有补偿操作？ → Saga
       │      └─ 无补偿（自动收敛）？ → CRDT
       └─ 否 → 重新审视业务是否真需要强一致
```

### 三、读写分离模式

解决读多写少或读写特征不同的问题。

| 模式 | 核心思路 | 典型技术 | 案例 |
|------|---------|---------|------|
| 推模型（写扩散） | 写入时推送到所有消费者 | Redis Pub/Sub | C03, C17 |
| 拉模型（读扩散） | 读取时聚合多个数据源 | 多路查询+合并 | C03 |
| 推拉结合 | 热点拉、冷点推 | 混合策略 | C03 |
| CQRS | 读写模型分离，写走事件、走物化视图 | ES/ReadDB | C07, C19 |
| 物化视图 | 预计算聚合结果，查询直接读取 | Continuous Aggregates | C05, C19 |
| 写时复制（COW） | 修改时创建新版本，不覆盖旧数据 | 事件溯源快照 | C06, C07 |

**推 vs 拉的决策矩阵：**
```
        写入少        写入多
读取少   拉模型        推模型（预计算省读取）
读取多   拉模型        推拉结合
```

### 四、存储分层模式

根据数据访问频率和时效性进行分层。

| 模式 | 层次结构 | 典型技术 | 案例 |
|------|---------|---------|------|
| 冷热分层 | Hot(SSD) → Warm(HDD) → Cold(对象存储) | 生命周期策略 | C05, C11 |
| 缓存穿透 | 本地缓存 → 分布式缓存 → 数据库 | Caffeine + Redis + MySQL | C01, C09 |
| 边缘缓存 | 边缘节点 → 区域中心 → 源站 | CDN + 区域存储 | C11, C23 |
| 列存+行存 | 行存(OLTP) + 列存(OLAP) | MySQL + ClickHouse | C19 |

### 五、实时处理模式

处理流式数据和实时计算的核心模式。

| 模式 | 核心思路 | 延迟 | 案例 |
|------|---------|------|------|
| 事件驱动 | 状态变更发布事件，消费者异步处理 | 毫秒-秒 | C02, C13, C14 |
| 流处理 | 无界数据流持续计算 | 毫秒 | C05, C08, C19 |
| Lambda | 实时层+离线层，离线校准实时 | 秒-天 | C19 |
| Kappa | 纯流处理，无离线层 | 秒 | （理论模式，C19中讨论局限） |
| 复杂事件处理(CEP) | 模式匹配、窗口聚合、时序关联 | 毫秒 | C05, C08 |
| 增量计算 | 只计算变化部分，不重算全量 | 毫秒 | C06, C19 |

### 六、弹性伸缩模式

应对流量波动的资源管理模式。

| 模式 | 核心思路 | 案例 |
|------|---------|------|
| 无状态水平扩展 | 请求可路由到任意实例 | C01, C10, C14 |
| 有状态优雅缩容 | 缩容只缩空闲实例，活跃实例不迁移 | C12 |
| 预测性伸缩 | 基于历史流量预测，提前扩容 | C21 |
| 请求排队 | 超出容量时排队等待，不丢弃 | C14, C21 |
| 限流降级 | 超出容量时快速失败 | C01 |

### 七、空间/位置模式

处理地理位置相关问题的模式。

| 模式 | 核心思路 | 技术 | 案例 |
|------|---------|------|------|
| 空间索引 | 将地理坐标编码为一维值 | GeoHash / S2 / H3 | C09 |
| 就近路由 | 请求路由到物理最近的节点 | Anycast / GeoDNS | C09, C23 |
| 空间分区 | 将空间划分为网格，按网格索引 | Geofence | C09, C15 |

### 八、安全与隔离模式

| 模式 | 核心思路 | 案例 |
|------|---------|------|
| 物理隔离 | 不同租户/数据使用独立硬件 | C04, C16 |
| 逻辑隔离 | 共享硬件，应用层/数据库层隔离 | C04 |
| 行级安全(RLS) | 数据库行级访问控制 | C04 |
| V8 Isolate | 轻量级进程隔离 | C23 |
| 零信任 | 不信任内网，每次请求都验证 | C18 |

## 模式组合速查

实际问题往往需要组合多个模式。以下是常见组合：

| 业务场景 | 核心问题 | 模式组合 |
|---------|---------|---------|
| 秒杀 | 流量洪峰+库存一致性 | 层层过滤+乐观锁+异步队列 |
| 社交Feed | 读写特征不同+大V扇出 | 推拉结合+冷热分层 |
| 跨境支付 | 强一致+多系统协调 | Saga+幂等+复式记账 |
| 协同编辑 | 并发冲突+离线支持 | CRDT+增量同步+快照 |
| 实时数仓 | 实时+离线口径一致 | Lambda+CQRS+物化视图 |
| 边缘CDN | 就近路由+缓存失效 | 空间索引+推送失效+请求合并 |

## 反模式（Anti-Patterns）警示

| 反模式 | 表现 | 正确做法 |
|--------|------|---------|
| 过度设计 | 简单问题用复杂架构 | 问题规模匹配方案复杂度 |
| 单点依赖 | 关键路径只有一个实例 | 识别单点，冗余部署 |
| 同步阻塞链 | 5个服务串行同步调用 | 异步化、并行化、缓存 |
| 缓存无策略 | 只加缓存不管失效和击穿 | 缓存穿透/击穿/雪崩防护 |
| 大而全的微服务 | 所有功能塞一个服务 | 按业务域拆分 |
| 无降级的强依赖 | 下游挂了整个链路不可用 | 熔断降级、优雅降级 |
| 数据库当消息队列 | 用轮询DB做异步任务 | 用真正的消息队列 |
| 忽略幂等 | 不处理重复请求 | 所有写操作必须幂等 |

---

> **如何使用模式图谱**：在遇到新问题时，先识别属于哪类模式，再从对应案例中找到参考方案，最后根据具体约束调整。模式是思考的起点，不是复制的终点。